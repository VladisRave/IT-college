# **Билеты по предмету "Основы проектирования баз данных (SQL)"**

Здесь представлен полный список билетов для сдачи дифференцируемого зачета по предмету **"Основы проектирования баз данных (SQL)"**.

### **Билет 1**
1.  **Глава 1:** Что такое транзакция в контексте работы с БД? Объясните, почему механизм транзакций критически важен для таких процессов, как, например, банковский перевод средств.
2.  **Глава 2:** Опишите операцию **соединения (JOIN)** в реляционной алгебре. Как с её помощью можно получить из двух таблиц с информацией о "Студентах" и "Курсах" единый список, показывающий, кто на каком курсе учится?

---

### **Билет 2**
1.  **Глава 2:** Объясните, чем **логическая независимость данных** упрощает жизнь разработчику приложения. Приведите пример: что может измениться в БД, и при этом не потребуется переписывать код приложения?
2.  **Глава 3:** Что такое **аномалия обновления** в ненормализованной таблице? Проиллюстрируйте на примере таблицы "Заказы", где дублируется информация о клиентах.

---

### **Билет 3**
1.  **Глава 3:** Объясните цель этапа **концептуального проектирования**. Почему создание ER-диаграммы (например, для системы бронирования отелей) предшествует написанию SQL-кода создания таблиц?
2.  **Глава 4:** Для чего используются **представления (VIEW)**? Приведите практический пример, где представление полезно для обеспечения безопасности данных (например, скрыть зарплату сотрудников от рядовых менеджеров).

---

### **Билет 4**
1.  **Глава 4:** Какие задачи решают **CASE-инструменты** (например, ERwin) при проектировании БД? Опишите процесс от создания диаграммы сущностей до генерации SQL-скриптов.
2.  **Глава 5:** В чём ключевая разница между операторами **`DELETE`** и **`TRUNCATE TABLE`**? В какой реальной ситуации (например, очистка временной таблицы в конце дня) предпочтительнее использовать `TRUNCATE`?

---

### **Билет 5**
1.  **Глава 5:** Что такое **ограничение `FOREIGN KEY`** (внешний ключ) и зачем оно нужно? Объясните на примере таблиц "Заказы" и "Клиенты", как это ограничение поддерживает целостность данных.
2.  **Глава 6:** Когда в запросе `SELECT` используется **`HAVING`**, а когда **`WHERE`**? Объясните на примере подсчёта среднего балла только для тех студентов, у которых он больше 4.0.

---

### **Билет 6**
1.  **Глава 6:** Объясните разницу между **`INNER JOIN`** и **`LEFT JOIN`**. На примере таблиц "Сотрудники" и "Проекты" покажите, как `LEFT JOIN` поможет найти всех сотрудников, даже тех, кто ещё не назначен ни на один проект.
2.  **Глава 1:** Опишите принцип **"клиент-серверной архитектуры"** в работе СУБД. Какая программа является "клиентом" (приведите пример), а какая — "сервером"?

---

### **Билет 7**
1.  **Глава 1:** Что подразумевается под **физической независимостью данных** в архитектуре ANSI/SPARC? Как это позволяет, например, заменить жесткий диск на SSD без изменения логики приложения?
2.  **Глава 2:** Дайте определение понятиям **"отношение" (таблица), "атрибут" (столбец), "кортеж" (строка)**. Как эти понятия соотносятся с терминами, используемыми в SQL при создании таблицы (`CREATE TABLE`)?

---

### **Билет 8**
1.  **Глава 2:** Какие типы **моделей данных** (кроме реляционной) вы знаете? Где может быть полезна иерархическая модель (приведите пример предметной области)?
2.  **Глава 3:** Что такое **первая нормальная форма (1NF)**? Приведите пример таблицы, нарушающей 1NF (например, хранение нескольких телефонных номеров в одной ячейке), и исправьте её.

---

### **Билет 9**
1.  **Глава 3:** Зачем нужен этап **логического проектирования** после концептуального? Как информация с ER-диаграммы о сущностях и связях преобразуется в схему реляционных таблиц?
2.  **Глава 4:** Как **представление (VIEW)** может упростить сложный запрос для конечного пользователя? Приведите пример VIEW, который скрывает сложность соединения 3-4 таблиц, предоставляя данные в виде одной виртуальной таблицы.

---

### **Билет 10**
1.  **Глава 4:** Почему проектирование интерфейса пользователя (форм, отчётов) часто рассматривают параллельно с проектированием структуры БД? Как неудобная форма ввода может привести к ошибкам в данных?
2.  **Глава 5:** Опишите разницу между операторами **`DROP TABLE`** и **`DELETE FROM table`**. Что произойдёт с ограничениями целостности (внешними ключами) в каждом случае?

---

### **Билет 11**
1.  **Глава 5:** Какие группы операторов входят в SQL (DDL, DML, DQL, DCL, TCL)? К какой группе относится оператор `CREATE INDEX` и какова его практическая цель для ускорения выборки данных?
2.  **Глава 6:** Для чего используется **агрегатная функция `COUNT()`** в сочетании с `GROUP BY`? Приведите пример запроса, который подсчитывает количество сотрудников в каждом отделе компании.

---

### **Билет 12**
1.  **Глава 6:** Что делает оператор **`ORDER BY`**? Как с его помощью можно отсортировать список товаров сначала по категории, а внутри категории — по убыванию цены?
2.  **Глава 1:** Объясните, чем **база данных (БД)** как набор файлов отличается от **системы управления базами данных (СУБД)** как программного комплекса. Может ли существовать БД без СУБД?

---

### **Билет 13**
1.  **Глава 1:** Что такое **журналирование (ведение лога)** в СУБД и как оно связано с обеспечением отказоустойчивости? Что происходит при восстановлении БД после сбоя?
2.  **Глава 2:** Что такое **домен атрибута** в реляционной модели? Как понятие домена соотносится с указанием типа данных (`INT`, `VARCHAR`, `DATE`) для столбца в SQL?

---

### **Билет 14**
1.  **Глава 2:** Опишите операцию **проекции (PROJECT)** в реляционной алгебре. Какой аналог этой операции в языке SQL (`SELECT`)?
2.  **Глава 3:** В чём суть **второй нормальной формы (2NF)** и от какой зависимости она избавляет? Проиллюстрируйте на примере таблицы "Заказ-Товар", где также хранится цена товара.

---

### **Билет 15**
1.  **Глава 3:** Почему этап **физического проектирования** следует за логическим? Какие решения на этом этапе (например, создание индексов) влияют на производительность будущих SQL-запросов?
2.  **Глава 4:** Может ли **CASE-инструмент** помочь в документировании уже существующей БД (реверс-инжиниринг)? Зачем это может понадобиться?

---

### **Билет 16**
1.  **Глава 4:** Как **представление (VIEW)** может обеспечить дополнительный уровень безопасности? Приведите пример создания VIEW, который показывает только публичные данные профиля пользователя, скрывая приватные.
2.  **Глава 5:** Для чего используется оператор **`ALTER TABLE`**? Какая типичная задача решается с его помощью при эволюции работающего приложения (например, добавление нового поля "Скидка" в таблицу "Клиенты")?

---

### **Билет 17**
1.  **Глава 5:** Что такое **ограничение `UNIQUE`**? Чем оно отличается от `PRIMARY KEY`? В каком сценарии (например, хранение email пользователей) оно применимо?
2.  **Глава 6:** Зачем в запросе `SELECT` используют **псевдонимы (алиасы, `AS`)** для таблиц и столбцов? Приведите пример запроса с `JOIN`, где алиасы необходимы для удобства чтения.

---

### **Билет 18**
1.  **Глава 6:** Что возвращает запрос с **`CROSS JOIN`** (декартово произведение)? В какой реальной, не учебной, ситуации такой запрос может быть полезен (например, генерация всех возможных комбинаций размеров и цветов товара)?
2.  **Глава 1:** Объясните разницу между **данными** и **информацией**. Как БД и СУБД преобразуют данные в информацию для пользователя?

---

### **Билет 19**
1.  **Глава 1:** Опишите уровни **внешний, концептуальный и внутренний** в архитектуре ANSI/SPARC. Как эта модель позволяет разным приложениям (бухгалтерия, отдел кадров) видеть одну и ту же БД по-разному?
2.  **Глава 2:** Что такое **первичный ключ (PRIMARY KEY)**? Почему в качестве первичного ключа часто используют суррогатный (искусственный) числовой идентификатор, а не, например, ФИО человека?

---

### **Билет 20**
1.  **Глава 2:** Опишите операцию **выборки (SELECT)** в реляционной алгебре. Как она связана с оператором `WHERE` в SQL?
2.  **Глава 3:** Что такое **транзитивная зависимость** и какая нормальная форма (3NF) её устраняет? Приведите пример из таблицы "Сотрудник" с полями: ID, ФИО, Отдел, Город_отдела.

---